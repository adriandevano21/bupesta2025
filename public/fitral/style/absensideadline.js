window.dataLayer = window.dataLayer || [];

        function gtag() {
            dataLayer.push(arguments);
        }
        gtag('js', new Date());

        gtag('config', 'G-61TSDP49BB');

function handleCircleClick(satker) {
    alert(satker);
}

// Modal Entri Absensis
function TambahDeadline() {
    $("#add-absensi-deadline").modal('show');
    // Tambahkan logika lain di sini (navigasi, form, dll.)
}

